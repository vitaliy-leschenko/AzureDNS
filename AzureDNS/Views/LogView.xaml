<UserControl x:Class="AzureDNS.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding ViewModel, RelativeSource={RelativeSource Self}}" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ListView ItemsSource="{Binding Path=Messages}" x:Name="list" BorderThickness="0">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Priority" DisplayMemberBinding="{Binding Path=Priority}" />
                <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Path=Category}" />
                <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Path=Message}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
